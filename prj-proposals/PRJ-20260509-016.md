# PixelPal 多Agent实际协作流程实现

## 状态
- Proposal ID: P-20260509-016
- 状态: approved_for_dev
- 创建时间: 2026-05-09
- 负责人: 小墨

---

## 1. 背景

V15（P-20260509-015）只实现了多Agent基础设施（Registry、Bus），但没有实现实际的协作流程。

**目标：** 实现OrchestratorAgent主导的任务分解和分发流程，ExecutorAgent接收任务并执行，ReviewerAgent审查结果。

---

## 2. 功能设计

### 2.1 OrchestratorAgent 完整实现

```typescript
// src/services/agents/orchestratorAgent.ts

import { agentBus, agentRegistry } from './agentRegistry'
import type { AgentMessage, Task, AgentConfig } from './types'
import { AgentType } from './types'

let taskCounter = 0

export class OrchestratorAgent {
  private id = 'orchestrator'
  private pendingTasks = new Map<string, Task>()
  private completedTasks = new Map<string, Task>()

  constructor() {
    // 订阅消息
    agentBus.subscribe(this.id, this.handleMessage.bind(this))
  }

  private async handleMessage(msg: AgentMessage): Promise<void> {
    switch (msg.type) {
      case 'task':
        await this.processUserRequest(msg.payload as { message: string })
        break
      case 'result':
        await this.handleResult(msg.payload as { taskId: string; result: unknown })
        break
      case 'error':
        await this.handleError(msg.payload as { taskId: string; error: string })
        break
    }
  }

  async processUserRequest(input: { message: string }): Promise<void> {
    const taskId = `task-${++taskCounter}`

    // 分析消息，决定子任务
    const subTasks = this.decomposeTask(taskId, input.message)

    // 保存任务状态
    subTasks.forEach(t => this.pendingTasks.set(t.id, t))

    // 广播任务给所有Executor
    for (const task of subTasks) {
      const executor = this.selectExecutor(task)
      task.assignedTo = executor.id

      await agentBus.send({
        from: this.id,
        to: executor.id,
        type: 'task',
        payload: task,
        timestamp: Date.now(),
        traceId: taskId,
      })
    }

    // 通知用户任务已开始
    await this.notifyProgress(`已分解为${subTasks.length}个子任务，开始执行...`)
  }

  private decomposeTask(parentId: string, message: string): Task[] {
    // 简单分解逻辑，实际应该用LLM
    const tasks: Task[] = []

    // 检测是否需要代码相关任务
    if (message.match(/code|代码|写|function|def |class /i)) {
      tasks.push({
        id: `${parentId}-code`,
        type: 'code_generation',
        description: 'Generate code based on request',
        inputs: { message },
        status: 'pending',
        createdAt: Date.now(),
        dependencies: [],
      })
    }

    // 检测是否需要审查任务
    if (message.match(/review|审查|检查|test|测试/i)) {
      tasks.push({
        id: `${parentId}-review`,
        type: 'code_review',
        description: 'Review generated code',
        inputs: { message },
        status: 'pending',
        createdAt: Date.now(),
        dependencies: tasks.length > 1 ? [`${parentId}-code`] : [],
      })
    }

    // 默认至少一个执行任务
    if (tasks.length === 0) {
      tasks.push({
        id: `${parentId}-exec`,
        type: 'general',
        description: message,
        inputs: { message },
        status: 'pending',
        createdAt: Date.now(),
        dependencies: [],
      })
    }

    return tasks
  }

  private selectExecutor(task: Task): AgentConfig {
    // 根据任务类型选择Executor
    const executors = agentRegistry.getByType(AgentType.EXECUTOR)
    return executors[0] || { id: 'executor', name: 'Default', type: AgentType.EXECUTOR, capabilities: [] } as AgentConfig
  }

  private async handleResult(payload: { taskId: string; result: unknown }): Promise<void> {
    const task = this.pendingTasks.get(payload.taskId)
    if (!task) return

    task.status = 'completed'
    task.result = payload.result
    task.completedAt = Date.now()
    this.pendingTasks.delete(payload.taskId)
    this.completedTasks.set(payload.taskId, task)

    // 检查是否所有任务都完成
    if (this.pendingTasks.size === 0) {
      await this.notifyProgress('所有任务已完成！')
      await this.sendToUser({
        type: 'final',
        message: `任务完成！结果: ${JSON.stringify(payload.result)}`,
      })
    } else {
      await this.notifyProgress(`还有${this.pendingTasks.size}个任务在执行...`)
    }
  }

  private async handleError(payload: { taskId: string; error: string }): Promise<void> {
    const task = this.pendingTasks.get(payload.taskId)
    if (!task) return

    task.status = 'failed'
    task.result = { error: payload.error }
    this.pendingTasks.delete(payload.taskId)

    await this.notifyProgress(`任务${task.id}失败: ${payload.error}`)

    if (this.pendingTasks.size === 0) {
      await this.sendToUser({
        type: 'error',
        message: `任务全部完成，但有错误: ${payload.error}`,
      })
    }
  }

  private async notifyProgress(msg: string): Promise<void> {
    await this.sendToUser({ type: 'progress', message: msg })
  }

  private async sendToUser(payload: { type: string; message: string }): Promise<void> {
    // 通过platformAdapter或直接console
    console.log('[Orchestrator]', payload.type, payload.message)
  }
}

export const orchestratorAgent = new OrchestratorAgent()
```

### 2.2 ExecutorAgent 完整实现

```typescript
// src/services/agents/executorAgent.ts

import { agentBus, agentRegistry } from './agentRegistry'
import type { AgentMessage, Task } from './types'
import { AgentType } from './types'

export class ExecutorAgent {
  private id = 'executor'

  constructor() {
    agentRegistry.register({
      id: this.id,
      name: '执行Agent',
      type: AgentType.EXECUTOR,
      capabilities: ['code_execution', 'api_calls', 'file_operations'],
    })

    agentBus.subscribe(this.id, this.handleMessage.bind(this))
  }

  private async handleMessage(msg: AgentMessage): Promise<void> {
    if (msg.type === 'task') {
      const task = msg.payload as Task
      await this.executeTask(task, msg.traceId)
    }
  }

  async executeTask(task: Task, traceId?: string): Promise<void> {
    try {
      task.status = 'in_progress'

      // 模拟执行（实际应该调用V64的agentExecutor）
      const result = await this.simulateExecution(task)

      // 发送结果给Orchestrator
      await agentBus.send({
        from: this.id,
        to: 'orchestrator',
        type: 'result',
        payload: { taskId: task.id, result },
        timestamp: Date.now(),
        traceId,
      })
    } catch (error) {
      // 发送错误给Orchestrator
      await agentBus.send({
        from: this.id,
        to: 'orchestrator',
        type: 'error',
        payload: { taskId: task.id, error: String(error) },
        timestamp: Date.now(),
        traceId,
      })
    }
  }

  private async simulateExecution(task: Task): Promise<unknown> {
    // 模拟执行延迟
    await new Promise(r => setTimeout(r, 500))

    switch (task.type) {
      case 'code_generation':
        return { code: `// Generated for: ${task.description}`, language: 'javascript' }
      case 'code_review':
        return { issues: [], passed: true }
      case 'general':
      default:
        return { output: `Executed: ${task.description}`, success: true }
    }
  }
}

export const executorAgent = new ExecutorAgent()
```

### 2.3 ReviewerAgent 完整实现

```typescript
// src/services/agents/reviewerAgent.ts

import { agentBus, agentRegistry } from './agentRegistry'
import type { AgentMessage, Task } from './types'
import { AgentType } from './types'

export class ReviewerAgent {
  private id = 'reviewer'

  constructor() {
    agentRegistry.register({
      id: this.id,
      name: '审查Agent',
      type: AgentType.REVIEWER,
      capabilities: ['code_review', 'quality_check', 'test_generation'],
    })

    agentBus.subscribe(this.id, this.handleMessage.bind(this))
  }

  private async handleMessage(msg: AgentMessage): Promise<void> {
    if (msg.type === 'task' && (msg.payload as Task).type === 'code_review') {
      const task = msg.payload as Task
      await this.review(task, msg.traceId)
    }
  }

  private async review(task: Task, traceId?: string): Promise<void> {
    try {
      task.status = 'in_progress'

      // 简单审查逻辑
      const result = {
        passed: true,
        score: 85,
        issues: [
          { severity: 'warning', message: '建议添加注释' },
        ],
        suggestion: '代码整体良好，可进一步优化命名',
      }

      await agentBus.send({
        from: this.id,
        to: 'orchestrator',
        type: 'result',
        payload: { taskId: task.id, result },
        timestamp: Date.now(),
        traceId,
      })
    } catch (error) {
      await agentBus.send({
        from: this.id,
        to: 'orchestrator',
        type: 'error',
        payload: { taskId: task.id, error: String(error) },
        timestamp: Date.now(),
        traceId,
      })
    }
  }
}

export const reviewerAgent = new ReviewerAgent()
```

### 2.4 初始化入口

```typescript
// src/services/agents/init.ts

import { orchestratorAgent } from './orchestratorAgent'
import { executorAgent } from './executorAgent'
import { reviewerAgent } from './reviewerAgent'

// 导出以确保实例化
export { orchestratorAgent, executorAgent, reviewerAgent }

// 初始化函数
export function initMultiAgentSystem(): void {
  console.log('[MultiAgent] Initializing...')
  // 导入即实例化
  void orchestratorAgent
  void executorAgent
  void reviewerAgent
  console.log('[MultiAgent] Ready')
}
```

### 2.5 App.tsx集成

在 App.tsx 或入口处：
```typescript
import { initMultiAgentSystem } from './services/agents/init'

useEffect(() => {
  initMultiAgentSystem()
}, [])
```

---

## 3. 消息流程图

```
用户消息
    ↓
OrchestratorAgent.processUserRequest()
    ↓ 分解任务
    ├─→ [task-1] → ExecutorAgent.executeTask() → result → Orchestrator
    ├─→ [task-2] → ExecutorAgent.executeTask() → result → Orchestrator
    └─→ [task-3] → ReviewerAgent.review() → result → Orchestrator
                                    ↓
                            检查所有任务完成
                                    ↓
                            通知用户最终结果
```

---

## 4. 验收标准

1. `npx vite build --mode production` 通过
2. OrchestratorAgent 能分解用户消息为多个子任务
3. ExecutorAgent 能接收并执行任务
4. ReviewerAgent 能审查代码
5. 任务结果能返回给Orchestrator
6. 无 console.error（仅允许 console.warn）
